# Normalize text files to LF in the repo.
* text=auto

# Corpus fixtures are byte-exact test inputs: no EOL conversion, ever.
# (The parser has its own CRLF-transport tolerance, tested separately.)
*.patch -text
